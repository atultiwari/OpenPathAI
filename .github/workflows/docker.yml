# Phase 18 — build both Dockerfiles on push to main; push to
# GHCR only when a GHCR_TOKEN secret is configured (so forks +
# PRs build but don't push).
#
# GitHub Actions does not accept `secrets.*` in step-level `if:`
# conditions in newer workflow validators, so the token presence
# is evaluated via a prior shell step that writes an output
# consumed by downstream `if:` expressions.
#
# Multi-platform (arm64) is out of scope for Phase 18 — amd64
# only. Adding arm64 later is a one-line `platforms:` change.

name: Docker

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-cpu:
    name: Build CPU image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Detect GHCR push capability
        id: ghcr
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          if [ -n "$GHCR_TOKEN" ]; then
            echo "can_push=true" >> "$GITHUB_OUTPUT"
          else
            echo "can_push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR (only when token present)
        if: steps.ghcr.outputs.can_push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Short SHA
        id: sha
        run: echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build (+ push when authenticated) — CPU
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.cpu
          push: ${{ steps.ghcr.outputs.can_push == 'true' }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/atultiwari/openpathai:cpu-latest
            ghcr.io/atultiwari/openpathai:cpu-${{ steps.sha.outputs.short }}

  build-gpu:
    name: Build GPU image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Detect GHCR push capability
        id: ghcr
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          if [ -n "$GHCR_TOKEN" ]; then
            echo "can_push=true" >> "$GITHUB_OUTPUT"
          else
            echo "can_push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR (only when token present)
        if: steps.ghcr.outputs.can_push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Short SHA
        id: sha
        run: echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build (+ push when authenticated) — GPU
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.gpu
          push: ${{ steps.ghcr.outputs.can_push == 'true' }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/atultiwari/openpathai:gpu-latest
            ghcr.io/atultiwari/openpathai:gpu-${{ steps.sha.outputs.short }}
