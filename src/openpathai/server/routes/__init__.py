"""FastAPI sub-routers for the Phase-19 backend.

Every router exposes a single ``router = APIRouter(...)`` module-level
symbol so :func:`openpathai.server.app.create_app` can mount them
uniformly.
"""

from __future__ import annotations

__all__: list[str] = []
